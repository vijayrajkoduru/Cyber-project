fastapi==0.116.1            # CVE bumps (was 0.115.0) — pulls patched starlette 0.47.x
uvicorn[standard]==0.32.0
pydantic==2.9.0
python-jose[cryptography]==3.4.0   # was 3.3.0 — fixes 5 CVEs
cryptography>=42.0.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.31    # was 0.0.12 — fixes 7 CVEs (multipart DoS)
requests==2.33.0            # was 2.32.3 — fixes CVE-2024-35195 / CVE-2026-25645
aiohttp==3.14.1             # was 3.10.10 — fixes 32 CVEs
dnspython==2.7.0
python-whois==0.9.4
PyYAML==6.0.2
bcrypt==3.2.2
SQLAlchemy==2.0.51          # data layer (Postgres engine + pooling)
psycopg2-binary==2.9.9      # PostgreSQL driver
alembic==1.13.2             # database migrations
mmh3==4.1.0  # MurmurHash3 — used by recon/favicon_hash for Shodan favicon pivoting
anthropic>=0.45.0
sentry-sdk[fastapi]>=2.0.0   # error monitoring (no-op unless SENTRY_DSN is set)
