# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python
__pycache__/
*.pyc
*.pyo

# Logs & PIDs
*.log
*.pid
.react-server.pid
uvicorn.out.log
react-server.out.log
react-server.err.log

# Claude internal
.claude/

# Temp backend files
backend_missing.py
backend_recon_missing.py
backend_p345.py

# Credentials / local notes
ruff
.env
*.env
users.db
*.db

# Self-healing autoloader snapshots
.last_known_good/
backups/
data/
last_known_good/

# VulnusLab build hygiene
.snapshots/
src/App.js.bak.*
_archive/
tools/recon_new/
.login_backups/
.safe-fallback-commit
src/.cache-buster.js

# Safety net snapshots (rollback.sh history — local-only)
.safety/

# Per-module backups (backup_recon.sh archives — local-only, can grow large)
.recon_backups/

# Local vulnerable-target lab (docker fixtures — not part of the app)
vulnerable-lab/

# Internal ops / build docs — kept locally, NOT published to the public repo.
# They expose the VPS IP, admin username, backup paths and internal notes.
DAILY.md
EVERYDAY-TODO.md
BACKUP-RESTORE.md
TEST-TARGETS.md
BUILD_MODULE_PLAYBOOK.md
FORGE_PLAYBOOK.md
STANDARDIZATION-SPEC.md
mobile_ruff.md
.scaffold_delete_recon.txt
.vl-foundry-scores.json
prompts/
