"""VulnusLab payments — Razorpay order/webhook helpers, plan catalogue,
transactional email, and paid-user provisioning. Underscore-prefixed so the
main.py autoloader skips it (these are libraries imported by endpoints/payment.py,
not scanner endpoints)."""
