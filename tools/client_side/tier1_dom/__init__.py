"""tier1_dom — Document Object Model attack surface probes.

Covers:
  - csp_bypass_audit      (Content-Security-Policy weakness classes)
  - xss_dom_sinks_audit   (source-to-sink dataflow over inline + ext JS)
  - postmessage_audit     (window.postMessage listener origin checks)
"""
