"""tools._payloads.osint — AI-curated OSINT wordlists.

Loader pattern matches recon/vuln/webapp — each list is its own module so
individual scanners only import what they need. Refresh cadence: quarterly
(90 days) — search engines change slowly.
"""
