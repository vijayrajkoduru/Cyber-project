"""supply_chain tier2_install - install/build-time script behavior audit (advisory).

ADVISORY-ONLY heuristic review of install/build manifests (setup.py, setup.cfg,
pyproject build hooks, package.json pre/postinstall) against a curated pattern
set. INFO/LOW only — these are human-review hints, never confirmed exploitation.
"""
